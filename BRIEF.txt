Id like to build a 3d interactive web-based version of Manuel Island Malta in which the architecture and natural elements are displayed schematically but animated. This platform should allow multiple users to make changes live to the 3d environment by excavating, infilling, planting, paving, placing outdoor furniture and buildings - a bit like how it would work in the sims. Id like to be able to design all the 3d elements myself and allow users to intervene on eachothers work once every set amount of time (ex. Every 10 minutes 1 change). I need the website to be super lightning fast, showing updates from other users interventions in real time while also allowing users to save their own private version of the environment to a public database. Ask relevant questions to create a prompt bible to develop this software and suggest the best ways of working.

Prompt: Multiplayer 3D Architectural Sandbox (Manuel Island)ContextI am building a web-based, real-time multiplayer 3D interactive environment for Manuel Island, Malta. The aesthetic is Schematic Clay/Paper-craft (matte white, soft shadows, animated natural elements). The goal is to allow users to collaborative "sculpt" the island and place architectural assets with a restricted "Token-based" intervention system.Tech Stack RequirementsFramework: React + Vite3D Engine: React Three Fiber (R3F) + Three.jsState Management: Zustand (for UI and Token state)Database/Real-time: Supabase (for persistent storage and real-time broadcast)Styling: Tailwind CSSCore Feature Specs to Implement1. Terrain System (Displacement Mapping)Create a large PlaneGeometry representing the island.Implement a "Sculpting Brush" that modifies the vertex $Y$ positions (Heightmap).Excavate: Lower vertices. Infill: Raise vertices.Optimization: Ensure terrain updates are efficient (use geometry.attributes.position.needsUpdate = true).2. Ghost-Snap Placement SystemCreate a PlacementManager component.Selection: User selects a GLB asset from a sidebar.Ghosting: While dragging, show a 50% transparent version of the model. Use a Raycaster to snap the ghost's $Y$ position to the current terrain height.Gizmo: Once clicked, show a TransformControls (from @react-three/drei) to allow the user to Rotate/Translate.Commit: An "OK" button that triggers the final placement.3. Token & Intervention Logic (The 10-Minute Rule)Implement a global timer/cooldown logic.Users have Action Points. Every 10 minutes (configurable by an Admin variable), they receive 1 point.Placing a building or sculpting a patch of terrain costs 1 point.If points = 0, the "Commit" action is disabled and the UI shows a countdown.4. Real-time Multiplayer (Instant Pop)Integrate Supabase Realtime.When a user commits a change, push the event: { type: 'ADD_OBJECT', asset: 'building_A', transform: matrix4 }.Other clients must listen for this event and instantly render the new object without a page refresh.5. Aesthetic (Paper-craft Style)Set up a Canvas with shadows.Use MeshStandardMaterial for all objects: color: "#ffffff", roughness: 1, metalness: 0.Add an AmbientLight and a DirectionalLight with a softShadows helper.Implement a simple "Stop-motion" animation for trees (e.g., a slight scale pulse every 0.5s).Instruction for Cursor/ClaudeScaffold the project structure focusing on the Canvas setup and Zustand store for tokens.Write the Terrain component with basic vertex manipulation logic.Write the Placement component that handles the Ghost-Snap and Gizmo interaction.Ensure the code is modular, separating the 3D components from the React UI overlays.